---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class:list={["text-[#c2b067] underline hover:opacity-80 transition-opacity", className]}>
  <slot />
</a>
