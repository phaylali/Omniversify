---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class:list={["w-full flex flex-col items-center justify-center relative p-8 box-border min-h-0", className]}>
  <slot />
</section>
