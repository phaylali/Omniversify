---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class:list={["w-full flex flex-col items-center justify-center relative p-8 sm:p-6 box-border", className]}>
  <slot />
</section>
